vich_uploader:
    db_driver: orm
    metadata:
        type: attribute

    mappings:
        certification:
            uri_prefix: /uploads/certification
            upload_destination: '%kernel.project_dir%/public/uploads/certification'
